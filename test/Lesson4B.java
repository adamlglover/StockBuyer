public class Lesson4B {
   // This is a lesson on operators continued

  int x = 1;
  int y = 2;
  int z;
 
 // addition
  z = x + y;
  z = (x + y);
  z = x + (x + y);
  z = (x + y) + (x + y);

// subtraction
  x = -1;
  z = x - y;
  z = x - (x);
  
  x = 1;
// multiplication
  z = x * y;

// division
  z = x / y;

// special operators
// multiplicative operator
 z *= x + y;  // this
 z = z * (x + y); // is also this

// Addative operator
 z += x;
 z = z + x;

// Division
 z /= y;
 z = z / y;

// subtraction
 z -= x - y;
 z = z - (x - y);

// Moduluos
 z %= z;
 z = z % x;

// all operators

// -=
// +=
// /=
// %=
// *=

}
